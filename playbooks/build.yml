- hosts: all
  gather_facts: true
  roles:
    - use-buildset-registry
  vars:
    container_command: docker

  tasks:
    - name: Print container images
      debug:
        var: container_images

    - name: Build images
      include_role:
        name: build-container-image

    - name: Return images to zuul
      include_tasks: _return-image.yml
      loop: "{{ container_images }}"
      loop_control:
        loop_var: zj_image
